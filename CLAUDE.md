# CLAUDE.md

Приложение для изучения европейского португальского (PT-PT) с карточками и интервальными
повторениями. Живёт на **https://pt.taylo.co**.

## Главное в двух словах

- Весь фронтенд — **один файл `index.html`**, без сборки, без зависимостей, без фреймворков.
  Открывается как есть и локально, и на хостинге.
- Хостинг — **GitHub Pages**, репозиторий `sccscsasd/pt-taylo`, ветка `main`, корень.
  `git push` = деплой, сборка занимает около минуты.
- Бэкенд — **Supabase**, проект `slyksrymqggaujstbocx` (организация taylo.co, Free).
- **Колода общая, прогресс личный.** Слова живут в `pt_words` — одна строка на слово
  для всех; в `pt_cards` у человека только прогресс и свои слова. Строка прогресса
  заводится при первой оценке карточки, а не при раздаче колоды.
- **Колод шесть, каждая включается отдельно**: A1, A2, B1, B2, C1 и «Свои слова». Последняя —
  единственная без строк в `pt_words`: её содержимое лежит у каждого в `pt_cards`.
- `localStorage` — только кеш: колода в общем ключе, личное под ключом пользователя.
- Интерфейс на русском, контент на португальском. **Только европейская норма**: бразильские
  варианты в карточки и заметки не добавляем.

## Раскладка репозитория

```
index.html                     всё приложение: разметка, стили, логика (словника внутри больше нет)
CNAME                          pt.taylo.co, задаёт домен для Pages
supabase/functions/ai/index.ts исходник Edge Function (прокси к OpenAI)
supabase/migrations/           SQL, уже применённый к базе
  001_pt_cards.sql             таблица карточек, RLS, триггер, перенос из pt_data
  002_pt_cards_level.sql       колонка level (CEFR)
  003_dedupe_and_word_ids.sql  чистка задвоенных карточек и переход на id по слову
  004_pt_cards_merge.sql       слияние правок по полям вместо «принять или отбросить»
  005_pt_cards_one_row_per_word.sql  чистка воскресших дублей и уникальность слова
  006_shared_deck_catalog.sql  pt_decks, pt_words, pt_user_decks, колонка own
  007_deck_a1_a2.sql           колода A1–A2 в общей базе — позже разделена на A1 и A2
  008_deck_b1.sql              колода B1, напечатана генератором
  009_deck_b2.sql              колода B2
  010_deck_a1.sql              колода A1 — половина бывшей A1–A2
  011_deck_a2.sql              колода A2 — вторая половина
  012_deck_manual.sql          «Свои слова»: строка в каталоге, слов в pt_words нет
  013_retire_deck_a1_a2.sql    подписки переведены, старая колода убрана из каталога
  014_deck_c1.sql              колода C1, напечатана генератором
  015_pt_cards_fav.sql         избранное: колонка fav и правило слияния для неё
vocabulario/                   колоды: формат авторства, из него печатается миграция
  deck2sql.py                  генератор: JSON колоды → миграция для pt_words
  b1b2-passo.py                шаг работы над колодой B1–B2: показать кандидатов, добавить карточки
  b1b2-rejeitadas.txt          слова из выгрузки B1–B2, просмотренные и не взятые
  lacunas.py                   сверка по темам и добор карточек в колоды по уровню
  temas.py                     расстановка тем у карточек B1 и B2
  c1.py                        наполнение колоды C1: отсев занятых слов, порции, чек-лист
  temas-verificacao.json       списки слов для сверки — чем проверяем полноту колод
  baralho-completo-B1.json     927 карточек — источник колоды b1
  baralho-completo-B2.json     705 карточек — источник колоды b2
  baralho-completo-C1.json     990 карточек — источник колоды c1
  temas-A1-A2.json             18 тем; уровень и тема для колод a1 и a2, файл общий
  baralho-completo-A1.json     746 карточек — источник колоды a1
  baralho-completo-A2.json     1127 карточек — источник колоды a2
  baralho-A1-*.json            те же карточки по темам
  camoes-A1-inventario.md      исходный инвентарь Camões, справочно
  README.md                    откуда взят словник и как он чистился
```

## Устройство index.html

Порядок в файле: `<style>` → разметка (`header`, `main` с четырьмя `section.view`, `nav`,
экран входа `#gate`) → один `<script>`.

Четыре экрана, в порядке вкладок: **Добавить** (`#v-add`), **Учить** (`#v-study`),
**Карточки** (`#v-cards`), **Настройки** (`#v-set`).

Каркас — не прокручиваемая страница, а приложение: `body` высотой `100dvh` и flex-колонкой,
`header` и `nav` с `flex:none`, прокручивается только `main`. Меню поэтому стоит на месте
без `position:fixed`, а прокрутка экрана — `$('main').scrollTop`, не `window.scrollTo`.

В шапке — строка счёта и полоска освоения (`#mastery`): зелёным выучено, синим в работе,
серым остальное. «К повтору» в строке — только просроченные повторения (`reps > 0`),
новые слова считаются отдельно и не светятся красным в меню. Числа склоняет `plural()`.

Логические блоки скрипта, сверху вниз:

1. хранилище и хелперы (`load`/`save`, `norm`, `wordId`, `esc`, `plural`), сборка
   карточек из колоды и прогресса (`deck`, `prog`, `cards`, `rebuild`, `touch`)
2. `callProxy` / `askAI` / `askVision` — запросы к модели через Edge Function
3. добавление слов: предпросмотр перевода, автоподсказки, разбор текста, скриншот и OCR
4. `TEMAS` — только имена и порядок 18 тем; сами слова, их уровень и тема
   приезжают из общей колоды
5. список карточек с ранжированным поиском, тренировка (SRS) с фильтром по темам
   и уровням, статистика, цель на день, модалка «Как идут дела», навигация
6. аккаунт, локальный кеш, синхронизация
7. инициализация в самом конце

**Три величины вместо одной.** `deck` — слова колод с сервера (общий кеш браузера),
`prog` — личные строки `pt_cards`, `cards` — сборка первого со вторым. Весь остальной код
читает `cards` и ничего не знает про источник. Менять карточку можно прямо в `cards`,
но сохраняет её `touch(c)`: он переносит правку в `prog` и метит строку к отправке.
Добавили или убрали карточку — нужен `rebuild()`.

### Экран «Добавить»

Одна строка ввода работает композером, как в чате:

- слово или несколько через запятую → предпросмотр перевода → «Добавить в колоду»;
- слово, которое уже заведено, к модели не идёт: показывается готовая карточка;
- скриншот прикрепляется прямо в строку (Ctrl+V, перетаскивание, значок картинки),
  кнопка справа превращается в «Распознать»;
- вставленный абзац уходит в разбор на слова. Текст от списка отличает не запятая —
  в живом португальском абзаце она почти всегда есть, — а `looksLikeText()`: несколько
  предложений, кусок между запятыми длиннее трёх слов или точка в конце. `o banco, a chave`
  остаётся списком, `Ontem fui ao mercado, comprei pão e queijo.` уходит в разбор.

Внизу экрана — **цель на день** (`#goalCard`): дуга, число выученных за сегодня, неделя
кружками, серия и рекорд. Считаются только переводы в «выучено»: `bumpDay()` зовётся
из `grade()` при оценке «Легко» и с `-1`, если слово в тот же день вернули в изучение.
Цель хранится в `settings.goal` (5–99, по умолчанию 15), в настройках стоит степпер
с вписываемым числом. Подпись под заголовком — `LEADS[номер дня % 30]`: индекс от даты,
поэтому строка не скачет при перерисовке блока.
История лежит в `settings.learned` (`{'ГГГГ-ММ-ДД': сколько}`, последние 120 дней) и
синхронизируется вместе с настройками; при расхождении устройств `pullSettings()` сливает
счётчики по максимуму, а не затирает. `seedToday()` восстанавливает сегодняшнее число
по `archivedAt`, если счётчика ещё нет.

### Модалка «Как идут дела»

Открывается из блока цели — по дуге с числом (прозрачная кнопка `#goalPeek` поверх дуги),
по кружкам недели и по строке серии. Правило простое: **факт о прогрессе открывает
историю, действие и настройка остаются собой**. Поэтому «Продолжить учить» по-прежнему
ведёт в тренировку, а «из 15 слов в день ›» — в настройки.

`#goalPeek` лежит под `.mid` по z-index, а `.mid` не ловит указатель (`pointer-events:none`),
кроме самой кнопки цели. Если тронуть эти три правила, перестанет нажиматься либо дуга,
либо подпись цели.

Три вида, переключаются сегментированной полоской, состояние в `statMode`:

| Вид | График | Что отвечает |
|---|---|---|
| Прогноз | накопительная кривая выученного, дальше пунктир до 100% | когда при нынешнем темпе закончится колода |
| Темп | столбики по дням за 30 дней, зелёные — где цель взята | сколько выходит в день и часто ли берётся цель |
| Повторения | столбики на 14 дней вперёд по `due` | какая нагрузка придёт, где затор |

Считается всё из того, что уже есть: `settings.learned` — история по дням, `archived` —
сколько выучено сейчас, `due` — что придёт вперёд. Ничего нового не хранится и не
синхронизируется. Темп (`pace(14)`) делится не на 14, а на прожитые дни от первой записи —
иначе у того, кто начал вчера, темп выходит в четырнадцать раз меньше настоящего.
Накопительная кривая (`histSeries()`) строится от сегодняшнего итога назад вычитанием
дневных счётчиков, поэтому правый конец кривой всегда сходится с числом в шапке.

Графики — свой SVG в `barsSVG()` и `planSVG()`, без библиотек, в тех же переменных темы.
Цвет держит общее правило: зелёное — пройденное, синее — то, что идёт, красным
только просроченные повторения.

Блок разбора (`#textSec`) — это **результат**, а не станок: исходный текст с переводом
и крестиком, слова из него, счётчик незнакомых, «Выбрать все», кнопка создания
появляется только когда что-то отмечено, созданные карточки показываются тут же.

### Экран «Карточки»

Карточка в списке показывает **уровень пометкой в правом верхнем углу** — тот же вид, что
на карточке тренировки: серая рамка у A1–A2, оранжевая у B1 и выше. Рисует её `cardBody()`,
поэтому пометка есть и в предпросмотре на «Добавить»; у своего слова уровня нет и пометки
не будет. Вид задан общим правилом `.lvl`, положение — контекстом (`#studyCard .lvl` слева,
`.card .lvl` справа). У `.card .pt` стоит `padding-right:42px` — без него длинное слово
вроде `o número de identificação fiscal` уехало бы под пометку.

Список рисуется **порциями по 60** (`CARD_PAGE`): целиком это 34 тысячи узлов и треть
секунды, а `renderCards()` зовётся в конце каждой синхронизации, то есть при каждом
возврате в приложение. В DOM держится только долистанное; следующая порция дорисовывается
обработчиком прокрутки `main`, когда до конца остаётся меньше 800 px (`CARD_TAIL`).
В хвосте стоит `#cardMore` — он же подпись «Ещё N карточек», он же место, перед которым
вставляется порция; когда список кончился, хвост убирается.

Перерисовка по синхронизации не должна сбрасывать чтение: при том же фильтре и поиске
`renderCards()` возвращает и число нарисованных карточек, и `main.scrollTop`. Смена
фильтра или поиска, наоборот, начинает список с первой порции.

### Экран «Настройки»

Четыре блока одной формы — заголовок `.lbl`, содержимое, при надобности подпись `.hint`.
Порядок **от широкого к узкому**, и это не случайность: **список колод** решает, что вы
учите (`renderDecks()`: строка на колоду, галка справа, нажатие правит `pt_user_decks`;
последнюю выключить нельзя), направление тренировки — как вас спрашивают (`.seg.big`,
состояние красит `paintDir()`), цель на день — сколько в день (`#dir` и `#goal` — оба свои,
нативных `select` в приложении нет), аккаунт — почта и ссылка «Выйти».

Колоды стоят первыми, потому что это единственная настройка без второго входа: новая колода
появляется выключенной, и включить её больше негде. У цели, наоборот, есть более заметный
дом на «Добавить» — дуга и ссылка «из 15 слов в день ›». Цена: список колод самый высокий
блок (около 460 px на шесть строк), поэтому на телефоне направление и цель уходят под сгиб.
На ссылку это не влияет: `$('#goalEdit')` делает `$('#goal').focus()`, а браузер сам
прокручивает поле в видимую часть — проверено, `main` уезжает на 273 px.

**Строка состояния синхронизации говорит, только когда есть что сказать.** «Синхронизирую…»,
«Отправляю изменения: N», «Нет сети», ошибка — видны; всё отправлено — `renderSyncState()`
ставит пустой текст и `hidden`, а время последней синхронизации остаётся в `title`. Постоянное
«Синхронизировано в 18:16» отвечало на вопрос, которого никто не задавал. Учтите при правке:
`.account .who span` стоит `display:block` и перебивает `[hidden]`, поэтому рядом лежит
`.account .who span[hidden]{display:none}` — без него строка не спрячется.

### Цвет

Акцентов два, и оба со смыслом: **синий — идёт, зелёный — сделано**. Дуга цели синяя,
пока цель не набрана, и зеленеет вместе с числом, когда набрана (`.goal.done`); кружки
недели серые и зелёные; в полоске освоения зелёное — выученное, синее — в работе.
Оранжевый только для заметок и уровней B1+, красный — «не помню» и счётчик повторов.

### Данные карточки

Карточка в памяти — это слово колоды плюс личная строка:

```js
// из pt_words (общее, у всех одинаковое)
{ id, deck, word, pos, ru, ex, exru, note, level, tema, sort }
// из pt_cards (личное)
{ id, own, word, created, due, interval, reps, lapses,
  archived, archivedAt,          // archived — «выучено», из показа убрано
  fav,                           // избранное: личная пометка, свой фильтр на тренировке
  deleted,                       // надгробие: слово колоды перестаёт показываться этому человеку
  rev }                          // время последней правки, по нему решаются расхождения
```

У **своего слова** (`own = true`) содержимое лежит в той же личной строке: `pos`, `ru`,
`ex`, `exru`, `note`, `level`, `query`. У слова колоды эти поля на сервер не уезжают —
содержимое принадлежит колоде, а не человеку. Это делает `toRow()`.

**`id` выводится из слова: `'w:' + norm(word)`** (см. `wordId()`), одинаково в колоде
и в личной строке. `norm()` в клиенте и `public.pt_norm()` в базе должны совпадать —
если правите один, правьте и второй. Случайные id были причиной задвоения колоды,
см. «Грабли».

`word` у существительных с артиклем: `o carro`, `a chave`. Сравнение слов — через `norm()`,
он снимает артикль и пунктуацию. В личной строке `word` дублируется намеренно: по нему
держится уникальность и по нему же читаются строки в базе глазами.

### Общая база слов

Колода — **одна на всех**: `pt_words`, строка на слово. У человека в `pt_cards` только
прогресс и свои слова. Копий колоды нет ни у кого, поэтому не бывает «у кого какой снимок»:
слово убрали из колоды — оно пропало у всех сразу.

Содержимое колод пишется руками в `vocabulario/*.json` — это формат авторства, с историей
и разбором правок в git. В базу оно попадает миграцией, которую печатает генератор:

```bash
python vocabulario/deck2sql.py a1 > supabase/migrations/00N_deck_a1.sql
```

Повторный прогон безопасен: содержимое обновляется, прогресс людей не трогается, а слово,
выпавшее из JSON, удаляется из базы (`delete … where updated_at < now()` внутри транзакции —
у нетронутых строк метка старее). Никакого списка `retired` больше нет.

**Как добавить колоду.** Положить рядом её JSON в том же формате, дописать в `DECKS`
внутри `deck2sql.py` запись (файл, имя, уровни, порядок; ключ `temas` только если у колоды
есть файл тем), прогнать генератор, выполнить миграцию. У всех в настройках появится
переключатель, **выключенный**: подписка на новые колоды даётся только тем, у кого
подписок нет вовсе. Клиент не меняется.

**Колода «Свои слова» устроена иначе.** Строк в `pt_words` у неё нет и генератор её
не печатает: слово, добавленное через экран «Добавить», живёт в личной строке `pt_cards`
с `own = true`. В каталоге она есть только затем, чтобы её можно было выключить.
Клиент знает её id: `rebuild()` показывает свои слова, лишь если человек подписан
на `manual`, а `renderDecks()` считает их по `prog`, а не по `pt_words`.

**Как добавить человека.** Завести аккаунт в Supabase (Authentication → Add user + Auto
Confirm). При первом входе клиент увидит, что подписок нет, и подпишет его на все
опубликованные колоды — `pullDecks()`. Строк в `pt_cards` у него будет ноль, пока он
не начнёт заниматься.

Файлы должны сходиться: `baralho-completo-A1.json`, `baralho-completo-A2.json`,
`temas-A1-A2.json` и то, что лежит в `pt_words`. Сверяет их сам генератор — он
предупреждает о словах без темы; у колод A1 и A2 таких больше нет.

**Чек-лист качества колоды.** Прогонять после каждого пополнения, всё проверяется
одним проходом по JSON:

1. совпадения `norm(word)` — их не должно быть вовсе, иначе две карточки схлопнутся
   в одну строку `pt_words`;
2. одинаковый `ru` у разных карточек — ловушка в режиме RU→PT;
3. `pos` против перевода: у существительного не должно быть глагольного перевода
   и наоборот, а два значения разных частей речи разводятся заметкой;
4. русский в переводах выражений — читать глазами, автоматика не ловит кальки
   вроде «иметься свободные номера»;
5. **заглавная буква в заглавном слове.** Континенты, страны, праздники и `Deus` —
   имена собственные и пишутся с большой; месяцы и дни недели, наоборот, со строчной,
   это норма португальского. 25.08.2026 нашлось семь карточек, где слово было строчным,
   а собственный пример на ней же писал его с большой (`a europa` при «da Europa»), —
   в режиме RU→PT колода учила неверной форме. Регистр на `id` не влияет: и `norm()`,
   и `pt_norm()` сначала приводят к нижнему, поэтому такую правку можно делать без
   опаски за прогресс.

**Редкость сама по себе — не повод удалять.** C1 по построению набиралась редким
регистром: вычистишь из неё всё редкое — не останется колоды. И «мне это не нужно»
решается не колодой, а крестиком на карточке: он ставит надгробие в личной строке,
слово исчезает у одного и остаётся у другого. Удаление из `pt_words` бьёт по всем сразу.
Полный пересмотр 25.08.2026 дал на 4506 карточек всего одиннадцать кандидатов, и убраны
они были по другой причине, чем редкость: `o fax` и `mandar um fax` — мёртвая технология
на самом начальном уровне, `a carta digital` — нишевая услуга CTT, `o dragão`, `a princesa`,
`o gigante`, `a bruxa` — сказочный пласт, бесполезный взрослому, `a antevéspera` и три
устаревающие идиомы из C1.

**Перевод должен соответствовать помете части речи.** Если у слова два значения
разных частей речи, на карточке остаётся то, что указано в `pos`, а второе уходит
в заметку: `universitário` — «университетский», а «студент вуза» в примечании.
Многозначность внутри одной части речи — это норма и полезно: `o banco` — «банк; скамейка».

**Единица массы — `o grama`,** мужского рода: `a grama` — это трава. В разговоре
в Португалии женский род распространён («duzentas gramas»), и это записано в заметке,
но заглавное слово держим по словарной норме — карточка учит артикль вместе со словом.

**Одинаковый перевод у двух карточек — ошибка.** В режиме RU→PT человек видит
«начало», а правильных ответов три (começo, início, princípio) — засчитывается один.
Синонимы разводим прямо в переводе: «начало, старт» против «начало (периода)».

Словник Camões оказался дырявым: в нём не было ни 85 ядровых глаголов (precisar,
dever, achar, encontrar, deixar, ajudar…), ни формул вежливости (olá, obrigado,
por favor, desculpa), ни базовых слов вроде amigo, pessoa, coisa, problema,
ни даже числительных от dois до vinte. Всё это дописано руками. Тематическая сверка
надёжнее частотной — стоит повторять её при любом пополнении колоды.

### Колоды B1 и B2

**Состояние на 25.08.2026: 927 карточек B1 и 705 B2.** Из них 910 написаны по выгрузке
(она просмотрена до конца, 2090 слов отклонено), остальные добраны тематической сверкой —
см. «Дыры в словниках». Обе колоды залиты в базу и появляются в настройках выключенными.

**Регистр B1–B2 добирался отдельно.** Выгрузка была перекошена в сторону конкретных
существительных, а уровень отличает как раз другое: абстрактная лексика (`a abordagem`,
`o critério`, `o impasse`), книжные связки (`ao passo que`, `por conseguinte`, `uma vez que`),
обороты (`ter a ver com`, `dar jeito`, `pôr em causa`), качества и черты характара
(`abrangente`, `teimoso`, `prudente`), цифровое (`o ecrã`, `o ficheiro`, `a palavra-passe`)
и разговорное (`fixe`, `se calhar`, `a chatice`). Списки V1–V10 в `temas-verificacao.json`
проверяют именно этот пласт; на 24.08.2026 пропусков по ним нет.

Источник — `portuguese_PTPT_B1_B2_3000_clean_candidate.xlsx` (лежит в `E:/other/chrome`,
в репозиторий не входит): 3000 лемм с пометой уровня и части речи, без переводов
и примеров. Выгрузка сырая, и это её главное свойство: в ней бразильские формы
(`cômodo`, `contato`, `aceito`, `aluguel`, `camiseta`), обрубки (`marquê`, `camponê`,
`bícep`, `víru`), словоформы вместо лемм (`desse`, `correndo`, `veremos`, `vc`, `ta`),
непристойное и редкости вроде `afasia` и `telecinesia`.

**Отбор только глазами.** Первую попытку я сделала скриптом со списками исключений —
он ошибся сразу, выбросив `a pá` и `o pó` как «слишком короткие». Списки убраны:
`b1b2-passo.py` ничего не решает, он лишь вычитает уже написанное и уже отклонённое
и печатает следующие 120 слов. Решение по каждому слову принимается вручную.

```bash
python vocabulario/b1b2-passo.py next   # показать следующие 120
python vocabulario/b1b2-passo.py add    # добавить порцию из _b1b2-porcao.py
```

Порция пишется в `vocabulario/_b1b2-porcao.py` (в репозиторий не входит) списком
кортежей `(слово, часть речи, уровень, перевод, пример, перевод примера, заметка)`.
`add` дописывает их в колоду, ругается на повторы, одинаковые переводы и примеры
не в диапазоне 7–14 слов, а всё показанное и не взятое заносит в `b1b2-rejeitadas.txt`.
**Показывать порцию нужно целиком:** если обрезать вывод, скрипт запишет в отклонённые
и то, чего никто не смотрел.

**Чек-лист качества колода проходит целиком** (проверено 24.08.2026): дублей по `norm(word)`
нет, пересечения с A1–A2 нет, одинаковых переводов внутри обеих колод нет, повторяющихся
примеров нет, все 910 примеров укладываются в 7–14 слов при медиане 8, заметок длиннее
90 символов нет, пустых полей нет. Дореформенных написаний не осталось: `nocturno`
в примере был единственным, `contacto`, `facto`, `adepto`, `impacto`, `expectativa`
согласные сохраняют по европейской норме.

### Колода C1

**Состояние на 25.08.2026: 990 карточек, ни одна не повторяет A1–B2.** Колода написана
руками в один заход и устроена как B1 и B2: файла тем у неё нет, уровень и тема лежат
в самой карточке. Уровень у всех один — C1, его проставляет скрипт. Миграция выполнена,
в каталоге колода стоит пятой (`sort` 50, «Свои слова» уехали на 60) и у всех выключена.

Выгрузки для этого уровня не было и не искали: словник на 3000 лемм уже показал себя
дырявым, а на C1 дело решает не частотность, а **регистр**. Поэтому колода набиралась
по пластам, а не по списку:

- книжная и газетная абстракция (`o âmago`, `a conjuntura`, `o pressuposto`, `a alçada`);
- связки письменной речи (`não obstante`, `porquanto`, `à revelia de`, `sem prejuízo de`);
- 199 глаголов, которых нет в младших колодах (`aferir`, `colmatar`, `escrutinar`,
  `preconizar`, `ressarcir`, `usufruir`);
- 84 прилагательных оценки и нюанса (`cabal`, `fidedigno`, `incontornável`, `peremptório`);
- практическая жизнь в Португалии: аренда и кондоминиум, налоги и `finanças`, суд
  и `contraordenação`, здравоохранение и `taxa moderadora`;
- еда и застолье как культурный слой (`o couvert`, `a imperial`, `o galão`, `a tasca`);
- 73 оборота и разговорных выражения (`ficar a ver navios`, `estar feito ao bife`,
  `pôr os pontos nos is`, `estar-se nas tintas`).

Инструмент — `c1.py`, устроен как `lacunas.py`: `add` не пишет ничего, пока есть хоть
одно замечание, и проверяет порцию против **всех пяти колод сразу**.

```bash
python vocabulario/c1.py novas    # какие слова из _c1-candidatas.txt ещё не заняты
python vocabulario/c1.py add      # добавить порцию из _c1-porcao.py
python vocabulario/c1.py stat     # темы, части речи, объём
python vocabulario/c1.py check    # чек-лист качества по всем колодам
```

Порция — кортежи `(слово, часть речи, тема, перевод, пример, перевод примера, заметка)`.
`novas` ничего не решает за человека: он лишь вычитает из списка кандидатов всё, что уже
занято любой колодой, — отбор по-прежнему глазами. Это отсеяло 242 слова из 1289
кандидатов, и ещё около сорока поймал `add` уже на готовых карточках.

**Чек-лист колода проходит целиком** (25.08.2026): повторов по `norm(word)` нет ни внутри
C1, ни против A1–B2; одинаковых переводов нет ни одного на 4495 карточек пяти колод;
повторяющихся примеров нет; все 990 примеров укладываются в 7–14 слов при медиане 8;
заметок 122, длиннее 90 символов нет; пустых полей и карточек без темы нет.

### Дыры в словниках

Выгрузка на 3000 слов оказалась дырявой так же, как словник Camões. Проверка показала:
из 120 ядровых слов, которых не было ни в одной колоде, по ошибке отклонено шесть
(`imóvel`, `cartório`, `cheia`, `evidente`, `histórico`, `agendar`), а **114 в выгрузке
отсутствовали вовсе**. В ней есть `telecinesia` и `afasia`, но нет `atingir`, `contrato`,
`greve`, `conselho`, `medo`. Отбор был почти правильный — дырявым был источник.

Инвентарь Camões, наоборот, колода A1–A2 покрывает целиком: все 1854 слова
из `temas-A1-A2.json` имеют карточку. Значит дыры лежат ровно за пределами обоих
словников, и найти их можно только тематической сверкой.

`temas-verificacao.json` — 21 список слов по темам словника плюс практическая жизнь
в Португалии. `lacunas.py check` печатает, чего из них нет ни в одной колоде;
на 24.08.2026 пропусков ноль, закрыто 395 карточек.

```bash
python vocabulario/lacunas.py check   # чего нет ни в одной колоде
python vocabulario/lacunas.py add     # добавить порцию из _lacunas-porcao.py
```

Порция — кортежи `(слово, часть речи, уровень, тема, перевод, пример, перевод примера,
заметка)`. **Уровень решает, в какую колоду ляжет карточка**: A1 и A2 уходят в колоду
A1–A2, и слово дописывается в `temas-A1-A2.json` в список `a1`/`a2` своей темы —
без этого генератор отдаст его в базу с пустыми `level` и `tema`. B1 и B2 уходят
в колоду B1–B2, тема хранится в самой карточке и генератором пока не используется.
В отличие от `b1b2-passo.py`, `add` не пишет ничего, пока есть хоть одно замечание:
колода A1–A2 уже в базе, чинить её потом дороже.

Сверка — не разовое дело. Пополнили колоду или заметили, что чего-то не хватает —
допишите слова в `temas-verificacao.json` и прогоните `check`.

**Темы у B1 и B2 лежат в самой карточке.** Выгрузка, из которой эти колоды написаны,
тем не знала, а файла вроде `temas-A1-A2.json` у них нет. Поэтому тема хранится в поле
`tema` рядом со словом и расставлена руками — глазами по слову и переводу, как и всё
остальное в этих колодах. Инструмент — `temas.py`:

```bash
python vocabulario/temas.py list 200   # карточки без темы
python vocabulario/temas.py set        # разложить порцию из _temas-porcao.py
python vocabulario/temas.py stat       # сколько карточек в какой теме
```

На 24.08.2026 карточек без темы нет ни в одной колоде.

Что осталось сделать: ничего срочного.

### Логика повторений

Интервалы фиксированные, без коэффициентов лёгкости:

| Кнопка | Что делает |
|---|---|
| Не помню | `interval=0`, показ через 10 минут, карточка возвращается в текущую сессию |
| Трудно | 1 день |
| Знаю | 14 дней |
| Легко | `archived=true` — «выучено», из тренировки убирается, слово остаётся в разделе «Выучено» |

Очередь перемешивается целиком (`shuffle`), дневного лимита новых слов нет.

Над карточкой — полоска фильтров: «Все», «Избранное», уровни (только те, по которым
есть слова), «Глаголы», разделитель, 18 тем словника и «Разное» для своих слов. Четыре
фильтра взаимоисключающие: `studyFav`, `studyLevel`, `studyVerbs`, `studyTema` — выбор
одного обнуляет остальные, отбор идёт через `inTema()`. «Глаголы» смотрят на `pos`
карточки.

### Избранное

Полоска фильтров умела только то, что задано колодой: уровень, часть речи, тема. Все три
признака принадлежат слову, а не человеку, и личную выборку — «эти тридцать слов мне нужны
к разговору в finanças» — выразить было нечем. Звёздочка это чинит.

Пометка — личное поле `fav` в `pt_cards`, у слова колоды оно едет на сервер вместе
с прогрессом (`toRow`). В `pt_words` её нет и быть не может: колода одна на всех.
Слияние — по `rev`, как у содержимого: обе стороны переключатель ставит человек осознанно,
поэтому побеждает свежая правка, и асимметрия, как у `archived`, здесь не нужна.

Ставится в двух местах, обе кнопки зовут `toggleFav()`:

- **на карточке тренировки** — звезда в правом верхнем углу. Уровень при этом переехал
  влево: он пассивная подпись, а звезда кнопка, и на телефоне ей место под большим пальцем,
  а не в зоне, где начинается свайп назад;
- **в списке «Карточки»** — звезда в ряду действий, и четвёртый сегмент фильтра над списком.

Оранжевый цвет — по общему правилу: синий «идёт», зелёный «сделано», оранжевый — то, что
человек пометил сам (им же покрашены заметки).

Два места, где легко сломать:

- пока ответ скрыт, **вся карточка — кнопка «показать»** (`$('#studyCard').onclick=reveal`).
  У звезды поэтому `stopPropagation`, иначе тап по ней заодно открывает ответ. Та же грабля,
  что с `#goalPeek` и z-index в блоке цели;
- **сегмент и чип показываются, пока в избранном что-то есть или пока фильтр выбран.**
  Второе условие обязательно: сняв последнюю звезду прямо в фильтре, человек иначе остался
  бы в нём без кнопки выхода. Видимость сегмента считает `paintFavSeg()`, и зовёт её
  `toggleFav()`, а не только `renderCards()`, — список пометка намеренно не перерисовывает,
  он рисуется порциями и перерисовка стоит прокрутки.

## Синхронизация

Истина на сервере, клиент работает от кеша и очереди. Синхронизируются **разные вещи
по-разному**: колода — только скачивается, личное — в обе стороны.

- **Колода.** `pullDecks()` тянет каталог и подписки (списки маленькие, целиком),
  `pullWords()` докачивает слова по метке `deckAt` — во второй раз не приезжает ничего.
  Кеш общий, не привязан к человеку: `pt.deck.v1`. Слово, выведенное из колоды, удаляется
  на сервере без надгробия, и по метке о нём не узнать, — поэтому `checkDeck()` сверяет
  число строк и при расхождении перечитывает колоду целиком.
- **Личное.** `prog` — строки `pt_cards`, `dirty` — id с неотправленными правками,
  `syncedAt` — метка сервера. Всё под ключом `pt.cache.v3.<user_id>`.
- любая правка зовёт `touch(c)` (переносит её в `prog`, ставит `rev`, метит id грязным),
  удаление — `dropCard(c)`: у слова колоды это надгробие в личной строке, сама колода цела.
- `flushCards()` отправляет очередь пачками по 300 строк (upsert по `(user_id,id)`),
  `pullCards()` докачивает изменения после `syncedAt` страницами до пустой.
- расхождения между устройствами решает **сервер**, а не клиент: триггер `pt_cards_touch()`
  сливает строку по полям (см. Supabase). `applyRow()` дополнительно не берёт строку
  со старым `rev` и не трогает то, что ещё не отправлено.
- без сети правки копятся, повторная отправка через 1,5 с после изменения и через 30 с
  после ошибки; отдельно отправка по `visibilitychange`, `pagehide` и событию `online`.

Запросы к базе идут через три помощника: `ask()` (ответ целиком, когда нужны заголовки),
`askJSON()` (разобранное тело) и `send()` (upsert). Страничная докачка — одна на всех,
`pullPages(table, since, apply)`: и слова колоды, и личные строки читаются ею.

## Supabase

Проект `slyksrymqggaujstbocx`, регион eu-central-1. Публикуемый ключ вшит в `index.html` —
так и задумано, без входа он бесполезен.

### Схема

| Объект | Назначение |
|---|---|
| `public.pt_decks` | **каталог колод**: `a1`, `a2`, `b1`, `b2`, `c1`, `manual`. Читают все вошедшие |
| `public.pt_words` | **общая база слов**: строка на слово для всех, `id = 'w:' + pt_norm(word)`, поля содержания плюс `level`, `tema`, `sort`. Читают все вошедшие, **политик на запись нет** — колоду меняет только миграция |
| `public.pt_user_decks` | кто какие колоды учит; человек правит только свои строки |
| `public.pt_cards` | **личное**: прогресс по словам колоды и свои слова (`own`), ключ `(user_id, id)`. `updated_at` ставит триггер, по ней клиент докачивает изменения; `rev` — версия по часам клиента; `fav` — избранное; `deleted` — надгробие. Уникальный индекс `pt_cards_one_row_per_word` на `(user_id, pt_norm(word)) where not deleted` |
| `public.pt_data` | только настройки: `data = {v:2, settings:{…}}`. Раньше здесь лежала вся колода одним jsonb |
| `public.pt_usage` | счётчик запросов к ИИ на пользователя в сутки |
| `public.pt_norm(text)` | нормализация слова как в клиентском `norm()`; на ней держится уникальность карточек |
| `public.pt_bump_usage(p_ip, p_limit)` | `security definer`, инкремент счётчика; анониму не выдан |
| `pt_words_backup_2026_08_25` | снимок колод перед выводом одиннадцати карточек 25.08.2026, 4506 слов |
| `pt_cards_orphans_2026_08_25` | шесть вычищенных строк-сирот, снятые перед удалением |

Снимки от 22 и 23 августа удалены 25.08.2026: события, которые они страховали, улеглись.
`drop table`, как и любой разрушающий SQL по этой базе, предохранитель не пропускает —
запрос выполняет владелец руками.

**Слияние правок живёт на сервере, а не в клиенте** (миграция 004). Триггер `pt_cards_touch()`
не отбрасывает и не принимает строку целиком, а сводит её по полям:

- `archived` снимает только копия со **свежим** `archived_at` — то есть настоящий возврат
  в изучение. Устаревшая копия приходит с нулём и выученное не отменяет;
- `reps` и `lapses` берутся по максимуму;
- содержимое, `due`, `ivl`, `fav`, `deleted` — по `rev`, старая версия не принимается;
- `updated_at` двигается **всегда**, даже когда правку не приняли, — иначе отправитель
  вычистит карточку из очереди и навсегда останется при своей копии.

Порядок отправки, офлайн и разница в часах после этого ничего не решают.

RLS на `pt_cards`: политика `for all to authenticated using (auth.uid() = user_id)`.
Проверено, что чужие строки не видны и не пишутся.

### Аутентификация

Публичная регистрация **отключена** (`disable_signup: true`), автоподтверждение почты
включено, минимальная длина пароля 6 (ниже Supabase не позволяет). Аккаунты заводит владелец:
Authentication → Users → Add user + Auto Confirm User. Колодой пользуются `artem@taylo.co`
и `yuliia@taylo.co`; в проекте есть ещё аккаунты от соседнего приложения — это нормально.

### Edge Function `ai`

Прокси к OpenAI, держит ключ в секретах (`OPENAI_API_KEY`, опционально `OPENAI_MODEL`,
`DAILY_LIMIT`). Принимает `{mode:"cards", items:[…]}` и `{mode:"vision", image:"data:…"}`.

Для режима `cards` модель возвращает `input`, `word`, `pos`, `ru`, `example_pt`,
`example_ru`, `level` (A1…C1) и `note`.

Проверяет токен **пользователя** через `/auth/v1/user`. Одного `verify_jwt` мало:
он пропускает и публикуемый ключ проекта, то есть без явной проверки прокси был бы открыт всем.

## Рабочие процессы

### Локальный запуск и проверка

```bash
python -m http.server 8777
```

Правки проверяю в браузере: пройти пару карточек, посмотреть консоль. Быстрее всего гонять
сценарии через `javascript_tool` — вызывать функции приложения напрямую (`showParse`,
`renderSugg`, `renderTemaBar`, `syncPush`) и читать `cards`/`dirty`/`localStorage`.
Если владелец в это время сам занимается в окне превью — проверять в **отдельной фоновой
вкладке**, а не в его.

Полный обход приложения делаю на **`127.0.0.1:8777`**, а не на `localhost:8777`: это другой
origin, значит своё пустое хранилище и экран входа вместо боевого аккаунта. Дальше в консоли
подставляю фиктивную сессию и глушу сеть, чтобы ни одна правка не уехала в Supabase:

```js
syncNow = async()=>{}; syncPush = async()=>{}; scheduleFlush = ()=>{};
auth = {access_token:'qa', refresh_token:'qa', expires_at:Date.now()+36e5,
        user:{id:'qa-local', email:'qa@local.test'}};
hideGate(); loadCache(); applySettings(); renderStats(); renderCards();
importCards(await (await fetch('vocabulario/baralho-completo-A1.json')).json());
```

После прогона — `localStorage.clear()`. Клавиши через автоматизацию до обработчиков не доходят
(Enter в строке ввода молчит), поэтому проверять их надо `dispatchEvent(new KeyboardEvent(...))`.
И помнить про переходы: `getComputedStyle` сразу после смены класса вернёт старый цвет,
`.goal.done` зеленеет за 0,3 с.

### Деплой

```bash
git add -A && git commit -m "…" && git push
```

Дальше жду `built` от `gh api repos/sccscsasd/pt-taylo/pages/builds/latest --jq .status`
и сверяю живой сайт с репозиторием:

```bash
curl -s https://pt.taylo.co/ | sha1sum; git show HEAD:index.html | sha1sum
```

### Изменения в Supabase

**SQL-редактор в панели ненадёжен** при программной подстановке: отправляет не то, что
подставлено в Monaco, и показывает устаревший результат. Рабочий путь — Management API
из вкладки с открытой панелью:

```js
const tok = JSON.parse(localStorage.getItem('supabase.dashboard.auth.token')).access_token;
await fetch('https://api.supabase.com/v1/projects/slyksrymqggaujstbocx/database/query', {
  method:'POST',
  headers:{'Content-Type':'application/json','Authorization':'Bearer '+tok},
  body: JSON.stringify({ query: '…' })
});
```

Токен панели живёт около часа: на `JWT failed verification` перезагрузить вкладку панели
и взять его заново. Результат запроса иногда возвращается пустым объектом — класть его
в `window.__x` и читать вторым вызовом.

**Миграцию колоды не надо вставлять в страницу текстом** — это сотни килобайт SQL. CSP
панели (`connect-src`) внешние адреса не пускает, но `https://raw.githubusercontent.com`
в список внесён. Поэтому: запушить миграцию, а потом из вкладки панели забрать её оттуда
и отправить в Management API одним запросом:

```js
const sql = await (await fetch('https://raw.githubusercontent.com/sccscsasd/pt-taylo/'
                             + 'main/supabase/migrations/014_deck_c1.sql')).text();
// дальше обычный POST на /database/query с этим sql
```

Токен панели наружу не отдаётся и не нужен нигде, кроме самой вкладки: сессия Claude
блокирует JWT в выводе, и это правильно.

Деплой функции — тем же токеном, multipart на `/functions/deploy?slug=ai`:

```js
const fd = new FormData();
fd.append('metadata', new Blob([JSON.stringify({entrypoint_path:'index.ts', name:'ai', verify_jwt:true})],
                               {type:'application/json'}));
fd.append('file', new File([bytes], 'index.ts', {type:'text/typescript'}));
fetch('https://api.supabase.com/v1/projects/slyksrymqggaujstbocx/functions/deploy?slug=ai',
      {method:'POST', headers:{'Authorization':'Bearer '+tok}, body: fd});
```

`GET /functions/ai/body` отдаёт собранный бандл, а не исходник, — патчить его на месте
нельзя, загружаем файл из репозитория.

После DDL PostgREST какое-то время отдаёт 404 на новую таблицу: нужен
`notify pgrst, 'reload schema';`.

## Грабли, на которые уже наступали

- **Устаревшая вкладка воскрешает дубли.** 23.08.2026 в `pt_cards` у владельца оказалось
  5056 строк вместо 1913: 1560 живых дублей со старым видом id и 1583 их надгробия. Все они
  созданы 22.08 до перехода на id по слову, а прилетели одним залпом 23.08 в 13:41 UTC —
  на телефоне осталась открытой вкладка с клиентом той версии и выплюнула свою очередь.
  Клиент схлопывает одинаковые слова и оставляет пришедшее последним, поэтому дубли
  заслонили выученные карточки: в шапке было «12 выучено» вместо 78, а цель на день
  показывала честные 35. **Прогресс при этом не терялся** — он лежал в каноничной строке.
  Лечилось миграцией 005: свести прогресс, убрать всё неканоничное, поставить уникальный
  индекс по `(user_id, pt_norm(word))`. Теперь такую строку сервер просто не примет.
  Если приложение снова покажет меньше выученных, чем вы помните, — первым делом
  `count(*)` против `count(distinct pt_norm(word))`.
- **Случайные id задваивали колоду.** Пока `importCards` выдавал каждой карточке `uid()`,
  два устройства делали два набора id для одних и тех же слов, и в `pt_cards` оказалось
  3277 строк вместо 1641. Клиент это маскировал: `applyRow()` схлопывает одинаковые слова
  при получении. Лечилось миграцией 003 и переходом на `wordId()`. Если снова появятся
  дубли — сначала смотреть `count(*)` против `count(distinct pt_norm(word))`.
- **Правка боевых данных во время занятий — гонка.** Пока чистил дубли, клиент владельца
  успел переотправить пять карточек под старыми id и воскресил их. Такие операции делать
  одной транзакцией и сразу проверять результат.
- **Разрушающий SQL блокирует классификатор.** `delete` и даже `update … set deleted = true`
  по боевой таблице не проходят без явного разрешения владельца. Просить разрешение прямо,
  предварительно сняв резервную таблицу.
- **Хвост в очереди синхронизации.** Если в `dirty` остаётся id карточки, которой уже нет,
  отправка крутится вхолостую и статус навсегда застревает на «Отправляю изменения».
  `flushCards()` теперь чистит такие id перед отправкой.
- **Меню не должно быть `position:fixed`.** Пока оно висело поверх страницы, его двигало
  всё подряд: полоса прокрутки на «Карточках» (15 px по горизонтали), адресная строка на
  телефоне. Каркас теперь такой: `body` высотой `100dvh` и `display:flex`, шапка и `nav`
  с `flex:none`, прокручивается только `main` (`flex:1; overflow-y:auto;
  scrollbar-gutter:stable`). Меню прижато к низу разметкой и не ездит вообще.
  Прокрутка экрана — это `$('main').scrollTop`, а не `window.scrollTo`.
- **localStorage привязан к origin.** `localhost`, `http://pt.taylo.co` и `https://pt.taylo.co` —
  три разных хранилища. Прогресс теперь в облаке, но при отладке об этом легко забыть.
- **localStorage — только кеш.** Ключ `pt.cache.v2.<user_id>` хранит копию колоды, очередь
  неотправленных правок (`dirty`, `tombs`) и метку `syncedAt`. Чистить его безопасно:
  всё вернётся с сервера. Опасно другое — почистить его, пока очередь не отправлена.
- **GitHub сам коммитит `CNAME`,** когда домен переустанавливают через API Pages. После этого
  `git push` отлетает с non-fast-forward — лечится `git pull --rebase`.
- **Сертификат Pages** может не выпускаться час и дольше. Помогает снять и заново поставить
  домен через API, после чего `https_certificate.state` переходит в `approved`.
- **Бесплатный Supabase засыпает** после недели без запросов: домен проекта перестаёт
  резолвиться, синхронизация отваливается. Данные целы, будится кнопкой Resume.
- **Стили полей перечисляют типы поимённо.** `input[type=text],[type=password],[type=email]` —
  и поле нового типа окажется без стилей: квадратные углы, чёрная рамка фокуса. Так полгода
  прожило «Почта» на экране входа. Добавляете поле — проверьте, что его тип есть в перечислении.
- **Тосты вместо ответа раздражают.** Сообщение «Такие карточки уже есть» скрывало готовый
  перевод, который лежал рядом. Если приложение знает ответ — показывать ответ, а не тост.

## Известные недочёты

Обход после переезда на общую базу слов, 23.08.2026: консоль чистая; колода и прогресс
собираются в 1829 карточек из 1829 слов и 8 личных строк; правка колоды на сервере доезжает
по метке; выведенное слово исчезает; своё слово уезжает с содержимым, слово колоды — только
прогрессом; второе устройство поднимает то же состояние с нуля; перезапуск без сети работает
из кеша. Осталось вот это, по убыванию важности.

**Чинить.**


**Стоит поправить.**

- **Разбор предлагает имена собственные.** На `…da estação de Santa Apolónia…` в незнакомые
  попадают `santa` и `apolónia`. Стоп-лист снимает служебные слова, а заглавную букву внутри
  предложения — нет, хотя регистр в исходном тексте ещё доступен до `toLowerCase()`.
- **Ошибка прокси показывается как «сервер ответил 401».** Функция отвечает `{error:…}`, но 401
  от шлюза Supabase приходит с полем `msg`, а `callProxy` (`index.html:637`) читает только
  `d.error`.
- **Цвет кнопок оценки спорит с правилом «синий — идёт, зелёный — сделано»**: «Легко» (выучено)
  синяя, «Знаю» (вернётся через 14 дней) зелёная.
- **320 px:** «Не помню» переносится в две строки и кнопка становится выше трёх соседних.
- **`maximum-scale=1`** в `index.html:5` запрещает зум щипком; Android Chrome это соблюдает.
- **В манифесте нет `icons`, нет `apple-touch-icon`** — на домашнем экране обобщённая иконка.
  `theme-color` жёстко чёрный, без варианта для светлой темы.
- **Экран входа — оверлей без `inert`/`aria-modal`:** Tab с поля пароля уводит на кнопки
  приложения под ним.
- **Скриншот на мобильном:** подпись обрезается — «Скриншот прикреплён — нажмит…».

**Знать и не удивляться.**

- После «Не помню» счётчик «осталось» не меняется: карточка уходит в конец очереди. В теме
  на 26 слов это честные «через 10 минут», на полной колоде — через 1800 карточек.
- **Строк, висящих вне колод, больше нет.** Шесть сирот от вывода одиннадцати карточек
  вычищены 25.08.2026 (запрос выполнил владелец руками — предохранитель разрушающий SQL
  по `pt_cards` не пропускает). В `pt_cards` 3489 строк, из них живых 3408.
- Запись про «79 слов у Юли, помеченных своими» оказалась неверной и убрана. Проверка
  25.08.2026 показала другое: строк вне колод 77, из них 76 у **владельца**, а не у Юли,
  и 74 из них давно надгробия. Живых на двоих три, и все они честные свои слова:
  `dar-me`, `o fumo` у одного и `passar a rua` у другой. Чистить нечего.
- Примеры в колодах A1 и A2 короче договорённых 7–14 слов: медиана 6, в диапазон попадают
  676 из 1873. В колодах B1, B2 и C1 в диапазон попадают все.
- Не проверено на боевых данных: первый вход нового человека (подписка на все колоды),
  живые ответы модели.

## Договорённости по контенту

- Только PT-PT. Бразильские соответствия не упоминаем ни в карточках, ни в заметках.
- Заметка — до 90 символов и по делу: род, неправильное мн. ч., нужный предлог, ложный друг,
  разговорный оттенок. Пустая строка лучше, чем вода.
- Пример — одно живое предложение на 7–14 слов уровня A2–B1 и естественный перевод,
  не подстрочник.
- Готовые колоды писались руками, а не моделью: `vocabulario/baralho-*.json`. Если добавляем
  B1–B2, держим тот же формат и складываем рядом — приложение готово выбирать колоду.
